---
name: "Description增强"
version: "2.0.0-general"
description: "角色卡转换的通用 Description 检查模块：仅归位原卡信息，并在存在明确证据时补齐；不预设性别、身体结构或专题内容。"
trigger: "与「角色卡转换」Skill 连带激活；受主 Skill §0 与 §1.0.1 统辖"
---

# Skill：Description 通用增强

## 零、优先级与适用边界

本模块是 `角色卡转换` 的通用补充模块，必须服从主 Skill：

1. §零 奥卡姆剃刀；
2. §1.0 忠于原卡；
3. §1.0.1 性别中性原则；
4. §1.0.2 专题增强边界。

若本模块与主 Skill 或专题 Skill 冲突：

```text
用户明确要求 > 主 Skill 元铁律 > 已显式激活的专题 Skill > 本模块
```

本模块只负责：

- 检查 `description` 中原卡已有信息是否完整归位；
- 从原卡其他字段搬运或概括有明确证据的内容；
- 删除重复、错误归位和 AI 审计痕迹；
- 保持不同角色的原始差异。

本模块不负责：

- 强制生成固定外貌骨架；
- 推断原卡未提及的性别、性取向、身体器官或性爱风格；
- 根据姓名、族裔、职业、体型、性格生成生理数值；
- 执行 Bara、兽人、机甲等专题深化。

---

## 一、触发条件

- 主 Skill 已激活；
- `description` 已完成语言转换；
- 字段拆分已完成；
- 当前内容尚未进入最终 V3 输出。

---

## 二、角色自适应维度

### 2.1 通用候选维度

以下均为**候选项，不是必须项**。只有原卡出现相关信息时才归位：

| 类别 | 候选维度 |
|------|----------|
| 身份 | 姓名、年龄、职业、阵营、物种、来源地、关系 |
| 外貌 | 体型、皮肤/材质、头发、面容、眼睛、穿着、配饰 |
| 非人类特征 | 耳、尾、角、翼、鳞片、外骨骼、机械部件、能源结构 |
| 身体细节 | 原卡明确描述的身体部位、伤疤、纹身、气味、体毛等 |
| 成人内容 | 原卡明确存在时才保留或归位；原卡未写则跳过 |
| 世界观 | 种族规则、能力限制、组织、时代与地点 |

### 2.2 禁止硬编码

不得要求所有角色都拥有或必须描述：

- 明确性别；
- 明确年龄数值；
- 明确国籍；
- 身高/体重数值；
- 肌肉群；
- 生殖器；
- 性爱风格；
- 体毛、气味或 NSFW 内容。

这些维度只有在原卡有直接文字、可靠上下文或用户明确要求时才处理。

---

## 三、检查与补齐规则

### 3.1 核心原则

> 有则归位，缺且有证据则概括；无证据则留白。

### 3.2 证据优先级

```text
P1 原 description 的明确陈述
P2 personality / scenario / first_mes / mes_example 的明确陈述
P3 tags / creator_notes / alternate_greetings 的一致性暗示
P4 原卡世界观中可直接推出且不存在多种解释的事实
P5 无可靠证据 → 不补
```

### 3.3 允许的补齐

- 将 `first_mes` 中明确写出的制服归入穿着；
- 将 `scenario` 中明确写出的职业归入身份；
- 将 `personality` 中反复出现的行为模式概括为性格；
- 将原卡多个字段一致写出的物种特征合并到 description；
- 将英日等原文准确翻译为中文，不增加新事实。

### 3.4 禁止的补齐

- 姓名像日文，所以补"日本籍"；
- 身材高大，所以补具体身高体重；
- 性格主导，所以补身体尺寸或性爱偏好；
- 族裔/肤色对应固定体毛、器官或气味；
- 男性角色默认补阴茎/睾丸；
- 女性角色默认补胸围/生殖结构；
- 机器人默认补能源核心，兽人默认补 Knot/Rut；
- 因专题模板存在就把所有角色写成同一体型或人格。

---

## 四、执行流程

```text
D1 解析 description 已有段落
D2 扫描原卡其他字段中的明确事实
D3 建立"事实 → 来源字段"内部映射（仅 tmp，不写入成品）
D4 将错位事实归入合适字段
D5 仅对有明确证据的缺失项做最小化概括
D6 去除重复、矛盾与 AI 决策痕迹
D7 多角色时分别处理，禁止互相复制特征
D8 交给主 Skill 执行奥卡姆自检与 V3 输出
```

---

## 五、输出要求

- 保留原卡的自由文本结构，不强制套用固定 Markdown 骨架；
- 可以重排段落，但不得删除原卡独有事实；
- 可以统一术语和标题，但不得把候选维度变成必填项；
- `creator_notes` 只保留原作者、角色定位或格式说明，不写转换记录；
- 推断依据、检查报告、字段映射只存于内部或 `/tmp`；
- 多角色卡中每个角色独立归位、独立检查。

---

## 六、与专题 Skill 的关系

专题 Skill 只有在用户明确选择或上下文明确触发时才运行。例如：

- `角色卡转换_Bara`：肌群、体毛、兽人等专题深化；
- 其他未来专题：机甲、怪物、世界书联动等。

专题 Skill 可以增加题材维度，但仍不得覆盖原卡明确设定。主 Skill 单独运行时，不得自动执行专题增强。

---

## 七、通用性自检

输出前回答：

- [ ] 是否仅使用了当前输入角色卡的信息？
- [ ] 是否没有继承上一角色的姓名、外貌、人格或数值？
- [ ] 是否没有默认性别、器官、性取向或 NSFW 内容？
- [ ] 是否没有按姓名、族裔、职业、体型做刻板数值推断？
- [ ] 是否保持了该角色独有的世界观与行为方式？
- [ ] 多角色是否分别处理，没有特征串卡？
- [ ] 无证据项是否留白而非补模板？

任一项为"否"，不得进入最终输出。